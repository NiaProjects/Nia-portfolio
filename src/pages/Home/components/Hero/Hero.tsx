import React from "react";
import Slider from "./Slider";

interface IProps {}

const Hero = ({}: IProps) => {
  return (
    <div className=" h-screen ">
      <Slider />
    </div>
  );
};

export default Hero;
