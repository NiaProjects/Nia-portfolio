import React from "react";
import SliderImages from "./components/SliderImages";

interface IProps {}

const Slider = ({}: IProps) => {
  return (
    <div>
      <SliderImages />
    </div>
  );
};

export default Slider;
